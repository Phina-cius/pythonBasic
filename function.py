#function to add two numbers
def add(y,x):
   rst=y+x
   return rst

#this is the function for subtraction
def subtract(y,x):
   rst=y-x
   return rst

def multiply(y,x):
   rst=y*x
   return rst

def divide(y,x):
   rst=y/x
   return rst








#parameter(variable) and arguement(number)
#calling the function
